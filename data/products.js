/* ================================================================
   DEPRECATED — products are now loaded automatically from Shopify.
   Use data/product-overrides.js for optional ribbons / custom images.
   Use data/shop-config.js for Shopify credentials + collection.
   ================================================================ */

window.ZC_PRODUCTS = [];
